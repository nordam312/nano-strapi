{
  "name": "nano-strapi",
  "version": "0.1.0",
  "description": "A tiny plugin-based framework that re-implements Strapi's core boot system: DI container, plugin loader, declarative routes, lifecycle, and onion middleware — with zero runtime dependencies.",
  "type": "module",
  "scripts": {
    "dev": "tsx src/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "keywords": [
    "framework",
    "plugin-system",
    "dependency-injection",
    "middleware",
    "typescript"
  ],
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.9"
  }
}
